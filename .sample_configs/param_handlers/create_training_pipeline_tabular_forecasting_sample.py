def make_parent(parent: str) -> str:
    parent = parent

    return parent

def make_training_pipeline(training_pipeline: google.cloud.aiplatform_v1beta1.types.training_pipeline.TrainingPipeline) -> google.cloud.aiplatform_v1beta1.types.training_pipeline.TrainingPipeline:
    training_pipeline = training_pipeline

    return training_pipeline

