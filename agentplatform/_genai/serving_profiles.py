# Copyright 2025 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

# Code generated by the Google Gen AI SDK generator DO NOT EDIT.

import json
import logging
from typing import Any, Optional, Union
from urllib.parse import urlencode

from google.genai import _api_module
from google.genai import _common
from google.genai._common import get_value_by_path as getv
from google.genai._common import set_value_by_path as setv
from google.genai.pagers import AsyncPager, Pager

from . import _operations_utils
from . import types

logger = logging.getLogger("agentplatform_genai.servingprofiles")


def _CreateServingProfileConfig_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}

    if getv(from_object, ["description"]) is not None:
        setv(parent_object, ["description"], getv(from_object, ["description"]))

    return to_object


def _CreateServingProfileRequestParameters_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}
    if getv(from_object, ["serving_profile_id"]) is not None:
        setv(
            to_object,
            ["_query", "servingProfileId"],
            getv(from_object, ["serving_profile_id"]),
        )

    if getv(from_object, ["display_name"]) is not None:
        setv(to_object, ["displayName"], getv(from_object, ["display_name"]))

    if getv(from_object, ["scope"]) is not None:
        setv(to_object, ["scope"], getv(from_object, ["scope"]))

    if getv(from_object, ["cmek_config"]) is not None:
        setv(to_object, ["cmekConfig"], getv(from_object, ["cmek_config"]))

    if getv(from_object, ["config"]) is not None:
        _CreateServingProfileConfig_to_vertex(getv(from_object, ["config"]), to_object)

    return to_object


def _DeleteServingProfileRequestParameters_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}
    if getv(from_object, ["name"]) is not None:
        setv(to_object, ["_url", "name"], getv(from_object, ["name"]))

    return to_object


def _GetServingProfileOperationParameters_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}
    if getv(from_object, ["operation_name"]) is not None:
        setv(
            to_object, ["_url", "operationName"], getv(from_object, ["operation_name"])
        )

    return to_object


def _GetServingProfileRequestParameters_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}
    if getv(from_object, ["name"]) is not None:
        setv(to_object, ["_url", "name"], getv(from_object, ["name"]))

    if getv(from_object, ["config"]) is not None:
        setv(to_object, ["config"], getv(from_object, ["config"]))

    return to_object


def _ListServingProfilesConfig_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}

    if getv(from_object, ["page_size"]) is not None:
        setv(parent_object, ["_query", "pageSize"], getv(from_object, ["page_size"]))

    if getv(from_object, ["page_token"]) is not None:
        setv(parent_object, ["_query", "pageToken"], getv(from_object, ["page_token"]))

    return to_object


def _ListServingProfilesRequestParameters_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}
    if getv(from_object, ["config"]) is not None:
        setv(
            to_object,
            ["config"],
            _ListServingProfilesConfig_to_vertex(
                getv(from_object, ["config"]), to_object
            ),
        )

    return to_object


def _UpdateServingProfileConfig_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}

    if getv(from_object, ["display_name"]) is not None:
        setv(parent_object, ["displayName"], getv(from_object, ["display_name"]))

    if getv(from_object, ["description"]) is not None:
        setv(parent_object, ["description"], getv(from_object, ["description"]))

    if getv(from_object, ["update_mask"]) is not None:
        setv(
            parent_object, ["_query", "updateMask"], getv(from_object, ["update_mask"])
        )

    return to_object


def _UpdateServingProfileRequestParameters_to_vertex(
    from_object: Union[dict[str, Any], object],
    parent_object: Optional[dict[str, Any]] = None,
) -> dict[str, Any]:
    to_object: dict[str, Any] = {}
    if getv(from_object, ["name"]) is not None:
        setv(to_object, ["_url", "name"], getv(from_object, ["name"]))

    if getv(from_object, ["config"]) is not None:
        _UpdateServingProfileConfig_to_vertex(getv(from_object, ["config"]), to_object)

    return to_object


class ServingProfiles(_api_module.BaseModule):
    """Class for managing ServingProfiles."""

    def get(
        self, *, name: str, config: Optional[types.GetServingProfileConfigOrDict] = None
    ) -> types.ServingProfile:
        """
        Gets a ServingProfile.
        """

        parameter_model = types._GetServingProfileRequestParameters(
            name=name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _GetServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{name}".format_map(request_url_dict)
            else:
                path = "{name}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = self._api_client.request("get", path, request_dict, http_options)

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfile._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    def _create(
        self,
        *,
        serving_profile_id: str,
        display_name: str,
        scope: types.ServingProfileScope,
        cmek_config: types.ServingProfileCmekConfigOrDict,
        config: Optional[types.CreateServingProfileConfigOrDict] = None,
    ) -> types.ServingProfileOperation:
        """
        Creates a new ServingProfile.
        """

        parameter_model = types._CreateServingProfileRequestParameters(
            serving_profile_id=serving_profile_id,
            display_name=display_name,
            scope=scope,
            cmek_config=cmek_config,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _CreateServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "servingProfiles".format_map(request_url_dict)
            else:
                path = "servingProfiles"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = self._api_client.request("post", path, request_dict, http_options)

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfileOperation._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    def _update(
        self,
        *,
        name: str,
        config: Optional[types.UpdateServingProfileConfigOrDict] = None,
    ) -> types.ServingProfile:
        """
        Updates a ServingProfile.
        """

        parameter_model = types._UpdateServingProfileRequestParameters(
            name=name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _UpdateServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{name}".format_map(request_url_dict)
            else:
                path = "{name}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = self._api_client.request("patch", path, request_dict, http_options)

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfile._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    def _list(
        self, *, config: Optional[types.ListServingProfilesConfigOrDict] = None
    ) -> types.ListServingProfilesResponse:
        parameter_model = types._ListServingProfilesRequestParameters(
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _ListServingProfilesRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "servingProfiles".format_map(request_url_dict)
            else:
                path = "servingProfiles"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = self._api_client.request("get", path, request_dict, http_options)

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ListServingProfilesResponse._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    def delete(
        self,
        *,
        name: str,
        config: Optional[types.DeleteServingProfileConfigOrDict] = None,
    ) -> None:
        """
        Deletes a ServingProfile.
        """

        parameter_model = types._DeleteServingProfileRequestParameters(
            name=name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _DeleteServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{name}".format_map(request_url_dict)
            else:
                path = "{name}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        self._api_client.request("delete", path, request_dict, http_options)

    def _get_serving_profile_operation(
        self,
        *,
        operation_name: str,
        config: Optional[types.GetServingProfileOperationConfigOrDict] = None,
    ) -> types.ServingProfileOperation:
        parameter_model = types._GetServingProfileOperationParameters(
            operation_name=operation_name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _GetServingProfileOperationParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{operationName}".format_map(request_url_dict)
            else:
                path = "{operationName}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = self._api_client.request("get", path, request_dict, http_options)

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfileOperation._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    def create(
        self,
        *,
        display_name: str,
        scope: Union[str, types.ServingProfileScope],
        serving_profile_id: str,
        cmek_config: Union[dict[str, Any], types.ServingProfileCmekConfig],
        config: Optional[types.CreateServingProfileConfigOrDict] = None,
    ) -> Union[types.ServingProfile, types.ServingProfileOperation]:
        """Creates a new ServingProfile.

        Args:
            display_name (str):
                Required. The display name of the ServingProfile.
            scope (Union[str, ServingProfileScope]):
                Required. The specific API this ServingProfile applies to.
            serving_profile_id (str):
                Required. The ID to use for the ServingProfile, which will become
                the final component of the ServingProfile's resource name.
            cmek_config (Union[dict[str, Any], ServingProfileCmekConfig]):
                Required. Customer-Managed Encryption Key (CMEK) configuration for
                the ServingProfile.
            config (CreateServingProfileConfigOrDict):
                Optional. The configuration for creating the ServingProfile.

        Returns:
            ServingProfile: The created ServingProfile if wait_for_completion is
            True.
            ServingProfileOperation: The operation for creating the ServingProfile
            if wait_for_completion is False.
        """
        if config is None:
            config = types.CreateServingProfileConfig()
        elif isinstance(config, dict):
            config = types.CreateServingProfileConfig.model_validate(config)
        elif not isinstance(config, types.CreateServingProfileConfig):
            raise TypeError(
                f"config must be a dict or CreateServingProfileConfig, but got {type(config)}."
            )

        operation = self._create(
            display_name=display_name,
            scope=scope,
            serving_profile_id=serving_profile_id,
            cmek_config=cmek_config,
            config=config,
        )

        if config.wait_for_completion:
            operation = _operations_utils.await_operation(
                operation_name=operation.name,
                get_operation_fn=self._get_serving_profile_operation,
            )
            if operation.error:
                raise RuntimeError(
                    f"Failed to create ServingProfile: {operation.error}"
                )
            return self.get(name=operation.response.name)

        return operation

    def update(
        self,
        *,
        name: str,
        config: Optional[types.UpdateServingProfileConfigOrDict] = None,
    ) -> types.ServingProfile:
        """Updates an existing ServingProfile.

        Args:
            name (str):
                Required. The resource name of the ServingProfile to update.
                Format:
                projects/{project}/locations/{location}/servingProfiles/{serving_profile}
            config (UpdateServingProfileConfigOrDict):
                Optional. The configuration for updating the ServingProfile.

        Returns:
            ServingProfile: The updated ServingProfile.
        """
        if config is None:
            config = types.UpdateServingProfileConfig()
        elif isinstance(config, dict):
            config = types.UpdateServingProfileConfig.model_validate(config)
        elif not isinstance(config, types.UpdateServingProfileConfig):
            raise TypeError(
                f"config must be a dict or UpdateServingProfileConfig, but got {type(config)}."
            )

        config = config.model_copy()

        update_mask_paths = []
        if config.display_name is not None:
            update_mask_paths.append("displayName")
        if config.description is not None:
            update_mask_paths.append("description")

        if not config.update_mask and update_mask_paths:
            config.update_mask = ",".join(update_mask_paths)

        if not config.update_mask:
            raise ValueError(
                "At least one field to update must be provided or specified via `update_mask`."
            )

        return self._update(name=name, config=config)

    def list(
        self, *, config: Optional[types.ListServingProfilesConfigOrDict] = None
    ) -> Pager[types.ServingProfile]:
        """
        Lists ServingProfiles in a Location.
        """

        list_request = self._list
        return Pager(
            "serving_profiles",
            list_request,
            self._list(config=config),
            config,
        )


class AsyncServingProfiles(_api_module.BaseModule):
    """Class for managing ServingProfiles."""

    async def get(
        self, *, name: str, config: Optional[types.GetServingProfileConfigOrDict] = None
    ) -> types.ServingProfile:
        """
        Gets a ServingProfile.
        """

        parameter_model = types._GetServingProfileRequestParameters(
            name=name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _GetServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{name}".format_map(request_url_dict)
            else:
                path = "{name}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = await self._api_client.async_request(
            "get", path, request_dict, http_options
        )

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfile._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    async def _create(
        self,
        *,
        serving_profile_id: str,
        display_name: str,
        scope: types.ServingProfileScope,
        cmek_config: types.ServingProfileCmekConfigOrDict,
        config: Optional[types.CreateServingProfileConfigOrDict] = None,
    ) -> types.ServingProfileOperation:
        """
        Creates a new ServingProfile.
        """

        parameter_model = types._CreateServingProfileRequestParameters(
            serving_profile_id=serving_profile_id,
            display_name=display_name,
            scope=scope,
            cmek_config=cmek_config,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _CreateServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "servingProfiles".format_map(request_url_dict)
            else:
                path = "servingProfiles"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = await self._api_client.async_request(
            "post", path, request_dict, http_options
        )

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfileOperation._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    async def _update(
        self,
        *,
        name: str,
        config: Optional[types.UpdateServingProfileConfigOrDict] = None,
    ) -> types.ServingProfile:
        """
        Updates a ServingProfile.
        """

        parameter_model = types._UpdateServingProfileRequestParameters(
            name=name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _UpdateServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{name}".format_map(request_url_dict)
            else:
                path = "{name}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = await self._api_client.async_request(
            "patch", path, request_dict, http_options
        )

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfile._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    async def _list(
        self, *, config: Optional[types.ListServingProfilesConfigOrDict] = None
    ) -> types.ListServingProfilesResponse:
        parameter_model = types._ListServingProfilesRequestParameters(
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _ListServingProfilesRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "servingProfiles".format_map(request_url_dict)
            else:
                path = "servingProfiles"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = await self._api_client.async_request(
            "get", path, request_dict, http_options
        )

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ListServingProfilesResponse._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    async def delete(
        self,
        *,
        name: str,
        config: Optional[types.DeleteServingProfileConfigOrDict] = None,
    ) -> None:
        """
        Deletes a ServingProfile.
        """

        parameter_model = types._DeleteServingProfileRequestParameters(
            name=name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _DeleteServingProfileRequestParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{name}".format_map(request_url_dict)
            else:
                path = "{name}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        await self._api_client.async_request("delete", path, request_dict, http_options)

    async def _get_serving_profile_operation(
        self,
        *,
        operation_name: str,
        config: Optional[types.GetServingProfileOperationConfigOrDict] = None,
    ) -> types.ServingProfileOperation:
        parameter_model = types._GetServingProfileOperationParameters(
            operation_name=operation_name,
            config=config,
        )

        request_url_dict: Optional[dict[str, str]]
        if not self._api_client.vertexai:
            raise ValueError(
                "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode."
            )
        else:
            request_dict = _GetServingProfileOperationParameters_to_vertex(
                parameter_model
            )
            request_url_dict = request_dict.get("_url")
            if request_url_dict:
                path = "{operationName}".format_map(request_url_dict)
            else:
                path = "{operationName}"

        query_params = request_dict.get("_query")
        if query_params:
            path = f"{path}?{urlencode(query_params)}"
        # TODO: remove the hack that pops config.
        request_dict.pop("config", None)

        http_options: Optional[types.HttpOptions] = None
        if (
            parameter_model.config is not None
            and parameter_model.config.http_options is not None
        ):
            http_options = parameter_model.config.http_options

        request_dict = _common.convert_to_dict(request_dict)
        request_dict = _common.encode_unserializable_types(request_dict)

        response = await self._api_client.async_request(
            "get", path, request_dict, http_options
        )

        response_dict = {} if not response.body else json.loads(response.body)

        return_value = types.ServingProfileOperation._from_response(
            response=response_dict,
            kwargs=(
                {
                    "config": {
                        "response_schema": getattr(
                            parameter_model.config, "response_schema", None
                        ),
                        "response_json_schema": getattr(
                            parameter_model.config, "response_json_schema", None
                        ),
                        "include_all_fields": getattr(
                            parameter_model.config, "include_all_fields", None
                        ),
                    }
                }
                if getattr(parameter_model, "config", None)
                else {}
            ),
        )

        self._api_client._verify_response(return_value)
        return return_value

    async def create(
        self,
        *,
        display_name: str,
        scope: Union[str, types.ServingProfileScope],
        serving_profile_id: str,
        cmek_config: Union[dict[str, Any], types.ServingProfileCmekConfig],
        config: Optional[types.CreateServingProfileConfigOrDict] = None,
    ) -> Union[types.ServingProfile, types.ServingProfileOperation]:
        """Creates a new ServingProfile asynchronously.

        Args:
            display_name (str):
                Required. The display name of the ServingProfile.
            scope (Union[str, ServingProfileScope]):
                Required. The specific API this ServingProfile applies to.
            serving_profile_id (str):
                Required. The ID to use for the ServingProfile, which will become
                the final component of the ServingProfile's resource name.
            cmek_config (Union[dict[str, Any], ServingProfileCmekConfig]):
                Required. Customer-Managed Encryption Key (CMEK) configuration for
                the ServingProfile.
            config (CreateServingProfileConfigOrDict):
                Optional. The configuration for creating the ServingProfile.

        Returns:
            ServingProfile: The created ServingProfile if wait_for_completion is
            True.
            ServingProfileOperation: The operation for creating the ServingProfile
            if wait_for_completion is False.
        """
        if config is None:
            config = types.CreateServingProfileConfig()
        elif isinstance(config, dict):
            config = types.CreateServingProfileConfig.model_validate(config)
        elif not isinstance(config, types.CreateServingProfileConfig):
            raise TypeError(
                f"config must be a dict or CreateServingProfileConfig, but got {type(config)}."
            )

        operation = await self._create(
            display_name=display_name,
            scope=scope,
            serving_profile_id=serving_profile_id,
            cmek_config=cmek_config,
            config=config,
        )

        if config.wait_for_completion:
            operation = await _operations_utils.await_operation_async(
                operation_name=operation.name,
                get_operation_fn=self._get_serving_profile_operation,
            )
            if operation.error:
                raise RuntimeError(
                    f"Failed to create ServingProfile: {operation.error}"
                )
            return await self.get(name=operation.response.name)

        return operation

    async def update(
        self,
        *,
        name: str,
        config: Optional[types.UpdateServingProfileConfigOrDict] = None,
    ) -> types.ServingProfile:
        """Updates an existing ServingProfile asynchronously.

        Args:
            name (str):
                Required. The resource name of the ServingProfile to update.
                Format:
                projects/{project}/locations/{location}/servingProfiles/{serving_profile}
            config (UpdateServingProfileConfigOrDict):
                Optional. The configuration for updating the ServingProfile.

        Returns:
            ServingProfile: The updated ServingProfile.
        """
        if config is None:
            config = types.UpdateServingProfileConfig()
        elif isinstance(config, dict):
            config = types.UpdateServingProfileConfig.model_validate(config)
        elif not isinstance(config, types.UpdateServingProfileConfig):
            raise TypeError(
                f"config must be a dict or UpdateServingProfileConfig, but got {type(config)}."
            )

        config = config.model_copy()

        update_mask_paths = []
        if config.display_name is not None:
            update_mask_paths.append("displayName")
        if config.description is not None:
            update_mask_paths.append("description")

        if not config.update_mask and update_mask_paths:
            config.update_mask = ",".join(update_mask_paths)

        if not config.update_mask:
            raise ValueError(
                "At least one field to update must be provided or specified via `update_mask`."
            )

        return await self._update(name=name, config=config)

    async def list(
        self, *, config: Optional[types.ListServingProfilesConfigOrDict] = None
    ) -> AsyncPager[types.ServingProfile]:
        list_request = self._list
        return AsyncPager(
            "serving_profiles",
            list_request,
            await self._list(config=config),
            config,
        )
